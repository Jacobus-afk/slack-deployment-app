[project]
name = "first-bolt-app"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "functions-framework>=3.8.3",
    "slack-bolt>=1.23.0",
]

[dependency-groups]
dev = [
    "uvicorn>=0.34.3",
]
